# F-034 — UC: ESLint configuration

Created: 2026-09-11 · Status: building (config landed; errors open)

## Goal
`npm run lint` runs a real ESLint config for Undercut and becomes a blocking hygiene check once its error-level findings are fixed.

## Acceptance criteria
- [x] `.eslintrc.js` at the root extends `expo` (legacy format: ESLint 8.57 is installed; `eslint-config-expo` 9's flat config needs ESLint 9). Ignores `newgame/`, `functions/`, build output and scripts.
- [ ] Fix the 4 error-level findings, all `react-hooks/rules-of-hooks`: `useRef`/`useEffect` called after an early return in `app/(tabs)/leagues/[id]/index.tsx:320-321` and `app/(tabs)/my-team/index.tsx:748-749`. Move the hooks above the early return (or split the component) — these can crash on re-render when the early-return condition flips.
- [ ] Decide on the 242 warnings (132 unused vars, 48 exhaustive-deps, 23 import/first, 21 no-require-imports, 9 array-type, 4 import/namespace): fix or downgrade rules deliberately.
- [ ] Set hygiene `lint: "npm run lint"` in `.aidlc/aidlc.yaml` and remove it from `advisory`.

## Targets
- undercut (root)

## Data
No user data.

## Performance budget
n/a

## Out of scope
- Track Limits lint (no config yet either; separate item).
