# F-036 — UC: Failing jest suites

Created: 2026-09-11 · Status: building (PR chore/undercut-gates)

## Goal
Make `npx jest --ci` a blocking hygiene check for Undercut. 20 of 240 tests failed: they encoded scoring rules from before the 2026-06 server-authoritative overhaul and an ace price cap that changed on 2026-02-21.

## Acceptance criteria
- [x] `src/services/scoring.service.ts` deleted with its barrel export (ROADMAP "Now" item, F-023): divergent, unused client scoring code. `scoring.service.test.ts` and `scoring.balance.test.ts` deleted with it; the two describe blocks in `league.team.test.ts` that exercised it removed. Server scoring lives in `functions/` and is verified by `functions/scripts/verifyRaceScoring.js`.
- [x] `ACE_MAX_PRICE` expectation updated from 240 to 200 in `pricing.config.test.ts` and `team.validation.test.ts` (config changed in commit 2184625, 2026-02-21).
- [x] `npx jest --ci` exits 0 (8 suites, 180 tests).
- [x] `.aidlc/aidlc.yaml` hygiene `test` runs jest before the Track Limits node:test suites.

## Targets
- undercut (root)

## Data
No user data.

## Performance budget
n/a

## Out of scope
- New tests for server scoring (F-024 scheduled verifier covers it).
