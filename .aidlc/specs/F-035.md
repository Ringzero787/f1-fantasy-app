# F-035 — UC: TypeScript errors and root tsconfig scope

Created: 2026-09-11 · Status: building (PR chore/undercut-gates)

## Goal
Make `npx tsc --noEmit` at the repo root a blocking hygiene check for Undercut. Before this the root tsconfig swept `newgame/` and `functions/` into Undercut's program (alias imports failed there) and Undercut itself had 12 real errors.

## Acceptance criteria
- [x] Root `tsconfig.json` excludes `newgame`, `functions`, `dist`, `android`, `android-bundle`, `undercut-site`.
- [x] `npx tsc --noEmit` exits 0 at the root. Fixes: `tabBarTestID` → `tabBarButtonTestID` (react-navigation 7 rename, 8 tabs; Maestro test ids unchanged); `PurchaseModal` exported from `src/components/index.ts` (it was imported by two league screens but never exported — those render paths threw at runtime); `expo-file-system` imported from `expo-file-system/legacy` in the four files using `EncodingType` (same runtime functions, no deprecation shim); `theme.text` → `COLORS.text.primary` and `COLORS.danger` → `COLORS.error` in `src/admin/AdminContent.tsx` (neither key existed).
- [x] `.aidlc/aidlc.yaml` hygiene `typecheck` includes the root run.
- [ ] Track Limits and its functions still typecheck (unchanged).

## Targets
- undercut (root)

## Data
No user data.

## Performance budget
n/a

## Out of scope
- ESLint errors (F-034). Jest (F-036).
